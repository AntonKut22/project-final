spring.cache.type: none
spring:
  sql:
    init:
      mode: always

  datasource:
    url: jdbc:h2:mem:jira;NON_KEYWORDS=KEY,VALUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driverClassName: org.h2.Driver
